---
const currentYear = new Date().getFullYear();

const links = {
  servicios: [
    { text: 'Consulta General', href: '#' },
    { text: 'Cirugía', href: '#' },
    { text: 'Vacunación', href: '#' },
    { text: 'Emergencias', href: '#' }
  ],
  horarios: [
    'Lunes - Viernes: 9:00 - 20:00',
    'Sábados: 10:00 - 15:00',
    'Domingos: Cerrado',
    'Emergencias: 24/7'
  ],
  legal: [
    { text: 'Política de Privacidad', href: '#' },
    { text: 'Términos y Condiciones', href: '#' },
    { text: 'Aviso Legal', href: '#' }
  ]
};
---

<footer class="bg-gray-900 text-gray-300 py-12">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      <div>
        <h3 class="text-2xl font-bold text-white mb-4">VetCare</h3>
        <p class="text-gray-400">Cuidado veterinario profesional y de calidad para tus mascotas.</p>
      </div>

      <div>
        <h4 class="text-lg font-semibold text-white mb-4">Servicios</h4>
        <ul class="space-y-2">
          {links.servicios.map(link => (
            <li>
              <a href={link.href} class="hover:text-primary-400 transition-colors">
                {link.text}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h4 class="text-lg font-semibold text-white mb-4">Horario</h4>
        <ul class="space-y-2">
          {links.horarios.map(horario => (
            <li>{horario}</li>
          ))}
        </ul>
      </div>

      <div>
        <h4 class="text-lg font-semibold text-white mb-4">Legal</h4>
        <ul class="space-y-2">
          {links.legal.map(link => (
            <li>
              <a href={link.href} class="hover:text-primary-400 transition-colors">
                {link.text}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="border-t border-gray-800 mt-12 pt-8 text-center">
      <p>&copy; {currentYear} VetCare. Todos los derechos reservados.</p>
    </div>
  </div>
</footer>